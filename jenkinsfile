node('Node1-JDK'){
    parameters { 
    choice(name: 'BRANCH', choices: ['main', 'REL_INT_1.0'], description: 'branch') }
    stage('vcs'){
        git branch: '${params.BRANCH}', url: 'https://github.com/yaminikor/spring-petclinic.git'

    }
    stage('build'){
        sh('mvn package')
    }
}